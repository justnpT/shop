var options = module.exports = {

    // headless: true,
    defaultViewport: {width: 1024, height: 760},
    devtools: true,
    timeout: 60000,
    maximumImplicitWaitTimeout: 900,
    middleImplicitWaitTimeout: 300,
    //TODO: puppeteer can be accessed from wdio
    // so use puppeteer as a service of wdio https://webdriver.io/docs/devtools-service.html#access-puppeteer-instance

}